server:
  port: 1901 # 指定该Eureka实例的端口号
eureka:
  instance:
    prefer-ip-address: true  # 是否显示主机的IP
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
      name: eureka-client-order  # 指定应用名称

